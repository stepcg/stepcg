/*
CSS assets manifest for this custom operator portal.

require the CSS from the default Front Office Manager (FOM) portal that this
portal inherits from
*= require ../portal

require all other CSS files within this custom Operator Portal's stylesheets folder
*= require_tree .

Include any CSS contained within this file HERE (after other required files):
*= require_self
*/

